# SNIK - Toolset, Maintainer: Konrad Höffner <konrad.hoeffner@imise.uni-leipzig.de>

services:

#  rdf:
#    build: ../ontology
#    volumes:
#      - rdf:/ontology

  virtuoso:
    build: ./virtuoso
    environment:
      - DBA_PASSWORD=${DBA_PASSWORD}
      - SPARQL_UPDATE='false'
    volumes:
      - virtuoso-data:/data
      - ../ontology:/data/toLoad:ro
    ports:
      - "127.0.0.1:8102:8890"
      - "127.0.0.1:8103:1111"
    restart: unless-stopped

  graph:
    environment:
      - SPARQL_ENDPOINT=http://127.0.0.1:8102/sparql
    build: ../graph

# Access at http://localhost:8104/ontology
  lodview:
    # subfolder, uses the master branch of https://github.com/snikproject/lodview
    # ignore the Dockerfile in the lodview Git repository, it is not used here and may have different settings
    build: ./lodview
    #environment:
      #- LODVIEW_URISPACE=http://www.snik.eu/ontology/
      #- LODVIEW_PREFIX=meta
      #LODVIEW_SPARQLENDPOINT: "http://virtuoso:8890/sparql"
      # for isolated local testing
      #- LODVIEW_SPARQLENDPOINT=https://www.snik.eu/sparql
      #- LODVIEW_HOMEURL=http://www.snik.eu/ontology/SoftwareProduct
      #- LODVIEW_HOME_TITLE=SNIK
      #- LODVIEW_HOME_ABSTRACT=This is the Abstract 
      #- LODVIEW_HOME_DESCRIPTION=RDF Browser for SNIK
      #- LODVIEW_PUBLICURLPREFIX=auto
    ports:
      - "127.0.0.1:8104:8080"
    depends_on: 
      - virtuoso
    restart: unless-stopped

volumes:
  rdf:
  virtuoso-data:

